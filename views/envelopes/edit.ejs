<!DOCTYPE html>
<html lang="en">

<%- include('../includes/head.ejs') %>

    <body>

        <%- include('../includes/nav.ejs') %>

            <div class="add-envelope content">
                <form action="/envelopes/edit/envelope/<%= envelope._id %>" method="POST">
                    <label for="category">Current Name: <%= envelope.category  %></label>
                    <input type="text" id="category" name="category" required>
                    <label for="budgeted">Amount Budgeted:  $<%= envelope.budgeted  %></label>
                    <input type="number" id="budgeted" name="budgeted" required>
                    <label for="balance">Amount Remaining:  $<%= envelope.balance  %></label>
                    <input type="number" id="balance" name="balance" required>
                    <button class="btn-primary btn-lg">Edit<div class="wrapper-button">
                        <div class="lid one-button"></div>
                        
                        <div class="envelope-button"></div>
                        
                    </div></button>
                </form>
            </div>
            <%- include('../includes/footer.ejs') %>
    </body>

</html>